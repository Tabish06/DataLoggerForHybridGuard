<%= form_with(model: log_load_time, local: true) do |form| %>
  <% if log_load_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(log_load_time.errors.count, "error") %> prohibited this log_load_time from being saved:</h2>

      <ul>
      <% log_load_time.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
